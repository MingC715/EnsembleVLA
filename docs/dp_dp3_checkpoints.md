# DP + DP3 Checkpoints

The checkpoint manifest has moved to `docs/checkpoints.md`, which records both released composition families:

- DP + DP3
- DP + pi0.5

This file is kept as a compatibility pointer for older references.
